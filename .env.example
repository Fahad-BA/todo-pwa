# PostgreSQL Configuration
# Replace these values with your actual PostgreSQL configuration

PG_USER=postgres
PG_HOST=localhost
PG_DATABASE=todo_app
PG_PASSWORD=password
PG_PORT=5432
PG_SSL=false

# Optional: Database connection string (overrides individual settings above)
# DATABASE_URL=postgresql://user:password@host:port/database